[pytest]
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
    concurrency: marks true parallel / race tests
    integration: marks tests hitting multiple layers or external resources
addopts = --cov=app --cov=tests --cov-report=term-missing --cov-fail-under=85
